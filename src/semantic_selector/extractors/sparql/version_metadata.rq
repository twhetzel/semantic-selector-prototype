PREFIX owl: <http://www.w3.org/2002/07/owl#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT DISTINCT ?versionPredicate ?versionValue
WHERE {
  VALUES ?versionPredicate {
    owl:versionIRI
    owl:versionInfo
    dcterms:modified
    dcterms:issued
  }
  ?s ?versionPredicate ?versionValue .
}
