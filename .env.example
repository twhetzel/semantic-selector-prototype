# Optional environment variables for Semantic Selector MVP
# SEMANTIC_SELECTOR_INDEX_PATH=data/indexes/selector-index.sqlite
# BIOPORTAL_API_KEY=  # future BioPortal adapter only; never commit real keys
